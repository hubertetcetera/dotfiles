# sketchybar

```bash
brew install --cask sf-symbols # so that symbols render correctly
brew tap FelixKratz/formulae
fc-list | grep -i "SF Pro" || echo "SF Pro not detected"

brew services restart sketchybar
```
